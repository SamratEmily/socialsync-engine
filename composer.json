{
    "name": "your-vendor/socialsync-engine",
    "description": "SocialSync Engine WordPress plugin",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.1",
        "guzzlehttp/guzzle": "^7.0"
    },
    "autoload": {
        "psr-4": {
            "SocialSyncEngine\\": "includes/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}
